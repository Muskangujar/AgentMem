"""
agentmem._grpc — Generated gRPC stubs placeholder.

In a production build, run:
    python -m grpc_tools.protoc -I../../core/proto \
        --python_out=. --grpc_python_out=. \
        ../../core/proto/agentmem.proto

This package provides the generated `agentmem_pb2` and `agentmem_pb2_grpc`
modules used by Memory's server mode.
"""
